org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
# org.gradle.java.home is set by toolchain in build.gradle
# Local path removed to allow CI/CD to use system Java

# MVS Development Environment Flag (Java equivalent of NODE_ENV=dev)
# Production default (used by CI/CD builds)
# For local development: create gradle.properties.local with development=true
development=false
